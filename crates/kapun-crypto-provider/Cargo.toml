[package]
name = "kapun-crypto-provider"
version = "0.1.0"
edition = "2024"

[dependencies]
uniffi = {workspace = true, optional = true}
kapun-util-rust = { path = "../../kapun-util/rust" }

[features]
default = []
uniffi = ["dep:uniffi"]
